import { Section, SectionHeader, Card } from "@/components/ui-kit";

const SocialProofSection = () => (
  <Section pad="md">
    <SectionHeader
      eyebrow="Early access"
      title="Built for the teams that own retention."
    />
    <Card className="p-12 text-center">
      <p className="text-secondary-foreground leading-relaxed">
        Placeholder for early access quotes and partner logos
      </p>
    </Card>
  </Section>
);

export default SocialProofSection;
