const SolutionSection = () => (
  <section className="py-24 md:py-32 relative">
    <div className="gradient-teal-glow absolute inset-0 pointer-events-none" />
    <div className="mx-auto max-w-[680px] px-6 relative text-center">
      <p className="text-xs font-medium text-primary uppercase tracking-[0.2em] mb-4">
        The solution
      </p>
      <h2 className="text-3xl md:text-4xl font-bold mb-6 leading-tight">
        Know every customer like they're your only one.
      </h2>
      <p className="text-lg text-secondary-foreground leading-relaxed mb-6">
        Cohvia connects to your CRM, call recordings, email, and support
        tickets — and turns all of it into a living account narrative for every
        customer. Always current. Never manual.
      </p>
      <p className="text-lg text-secondary-foreground leading-relaxed">
        Your team stops managing data and starts cultivating real relationships.
      </p>
    </div>
  </section>
);

export default SolutionSection;
